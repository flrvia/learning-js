console.log("Arithmetic Operations");

console.log(10 + 8 * 2);

// -> It will print 26 because it does the multiplication first, so we need to highlight what we want first, like:

console.log((10 + 8) * 2);

